name: Build Vivify Mod

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Android NDK
      uses: nttld/setup-ndk@v1
      with:
        ndk-version: r21e
    
    - name: Install QPM
      run: |
        wget -O qpm.zip https://github.com/QuestPackageManager/QPM.CLI/releases/latest/download/qpm-linux.zip
        unzip qpm.zip
        chmod +x qpm
        sudo mv qpm /usr/local/bin/
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake ninja-build
    
    - name: Restore dependencies
      run: qpm restore
    
    - name: Build
      run: |
        mkdir build
        cd build
        cmake ..
        cmake --build .
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: mod
        path: build/*.qmod
